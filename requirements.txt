# Agent
strands-agents>=1.0
strands-agents-tools>=0.1
pydantic>=2.7

# Data + documents
pypdf>=4.2
reportlab>=4.1

# Review queue UI
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1

# Tests
pytest>=8.0

# Optional: run against the Anthropic API instead of Bedrock
# strands-agents[anthropic]>=1.0

# Optional: AgentCore Runtime deployment
# bedrock-agentcore>=0.1
